# Base URL of the running Campus Genius FastAPI backend.
# Must match a CORS-allowed origin's counterpart - i.e. run this frontend
# on port 5173 or 3000 unless you also update backend/.env cors_origins.
VITE_API_BASE_URL=http://localhost:8000
